displayName=PriceCheck Flipping
author=PriceCheck.gg
description=Free exact flip tracker (profit, open positions, GE history import) plus PriceCheck's live flip board, offer advisor and GE click-to-fill for subscribers.
tags=flip,flipping,merch,tracker,profit,margin,grand exchange,ge,money,pricecheck
plugins=gg.pricecheck.runelite.PriceCheckPlugin
warning=Optional features submit data to PriceCheck's servers (not controlled or verified by the RuneLite Developers). Each is off by default and states exactly what it sends before enabling. Nothing is sent without a plugin key.
